---
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/domestic-floor-cleaning', label: 'Domestic' },
  { href: '/commercial-floor-cleaning', label: 'Commercial' },
  { href: '/contact', label: 'Contact' },
];

const currentPath = Astro.url.pathname;
---

<!-- Promo Banner -->
<div class="bg-primary text-white py-2 text-center text-sm">
  <p>Tackling tough stains is no problem with our spot stain treatment, starting from just <strong>£10</strong> per stain for a cleaner, fresher home</p>
</div>

<header class="bg-gray-100 shadow-sm sticky top-0 z-50">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16 md:h-20">
      <a href="/" class="flex items-center">
        <img src="/images/header-logo.png" alt="Absolutely Floorless" class="h-8 md:h-10 w-auto" />
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`text-sm font-medium transition-colors hover:text-secondary ${
              currentPath === item.href ? 'text-secondary' : 'text-gray-700'
            }`}
          >
            {item.label}
          </a>
        ))}
        <a
          href="tel:07481597929"
          class="bg-secondary text-white px-4 py-2 rounded-lg font-medium hover:bg-secondary/90 transition-colors"
        >
          07481 597929
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 rounded-lg hover:bg-gray-100"
        aria-label="Toggle menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden pb-4">
      <div class="flex flex-col space-y-2">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`px-4 py-2 rounded-lg font-medium transition-colors ${
              currentPath === item.href ? 'bg-secondary/10 text-secondary' : 'text-gray-700 hover:bg-gray-100'
            }`}
          >
            {item.label}
          </a>
        ))}
        <a
          href="tel:07481597929"
          class="bg-secondary text-white px-4 py-2 rounded-lg font-medium text-center hover:bg-secondary/90 transition-colors mt-2"
        >
          Call: 07481 597929
        </a>
      </div>
    </div>
  </nav>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');

  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });
</script>
